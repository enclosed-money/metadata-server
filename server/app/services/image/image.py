import cv2


def generate_image(token_id: str):
    deposited_token_address = token_id[:42]
    deposited_value = token_id[42:]

    # from token address get token name

    # load base ticket image

    # place token name, token address and deposited value text on base ticket image

    # return png image
    pass
